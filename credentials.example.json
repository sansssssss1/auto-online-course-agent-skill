{
  "$comment": "凭据模板：复制本文件为 credentials.local.json 后填入真值。credentials.local.json 已被 .gitignore 覆盖，绝不入库、绝不写入日志/状态文件。环境变量优先级更高：CHAOXING_PHONE/CHAOXING_PASSWORD、MOOC_PHONE/MOOC_PASSWORD。监督者（agent）在登录前向用户获取账号密码时，就写进 credentials.local.json。",
  "chaoxing": {
    "accounts": [
      { "name": "main", "phone": "<手机号/学号/工号>", "password": "<密码>" }
    ]
  },
  "icourse163": {
    "accounts": [
      { "name": "main", "phone": "<手机号>", "password": "<密码>" }
    ]
  }
}
