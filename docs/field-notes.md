# 现场日志（Field Notes）

> 规则：**只追加，不修改**。每条带日期与页面环境；报错/弹窗文字要原文引用；
> 这是本项目的测试用例库，是最值钱的资产。
> 历史来源：桌面《学习通网课挂机-进度与上下文总结.md》已验证条目（2026-09-11 迁移）。

## 条目模板

```markdown
### YYYY-MM-DD HH:mm — [节点/页面]
- 环境：（URL 关键参数 / 哪个 iframe 层）
- 现象：
- 原文引用：（报错/弹窗文字原样贴）
- 根因：（如已知）
- 解决/绕过：
- 状态：已解决 / 未解决 / 需复现
```

## 已验证条目（迁移自 2026-09-11 实测）

### 2026-09-11 — 目录页章节点击
- 现象：目录节点被透明 `<a>` 遮罩覆盖，常规自动化点击（坐标命中 `<a>`）无效果/超时
- 解决：对 `div.chapter_item#cur{id}` 用程序化 `el.click()`，让 click 冒泡到 `toOld(...)` onclick
- 状态：已解决

### 2026-09-11 — 视频 preload="none" 死锁
- 现象：保活脚本等待 `readyState>=2` 才 play，但 preload=none 的视频永远不加载，死锁
- 根因：加载由 play() 触发，等待加载再 play 是循环依赖
- 解决：保活脚本 v2 无条件尝试 `play()`，显式调用一次即开始加载
- 状态：已解决

### 2026-09-11 — 进入测验节点时的"任务点未完成"弹窗
- 现象：切到测验节点时弹「当前章节还有任务点未完成，是否去完成？」
- 解决：调用页面自身的 `closeDeleteWindow()` 关闭；注意页面有 8 个同名 `.popClose`，
  必须限定作用域，不能全局 querySelector
- 状态：已解决

### 2026-09-11 — 测验题目字体混淆（反爬）
- 现象：测验页 DOM 里题目文字是乱码/私有字体编码，直接读 DOM 不可用
- 解决（浏览器路线）：截图读渲染后的文字；渲染结果正常可读
- 备选（接口路线）：字体映射表解法，见 knowledge/chaoxing.md「字体混淆」一节
- 状态：已解决（浏览器路线）

### 2026-09-11 — 每日视频任务点完成上限（隐藏提示框）
- 现象：DOM 中存在隐藏提示框（class `maskDiv jobCountDiv`）：
  「今日视频任务点完成数已达上限，您可以观看视频，但无法完成任务点」
- 根因：平台级防挂机限制；触发后当天视频不再计进度
- 解决：触发即停止当日视频任务，整理状态收工，次日续跑
- 状态：未触发过（预防性知识），持续观察

### 2026-09-11 — 2 倍速进度有效性确认
- 现象：2x 静音播放 + 自动下一节，右侧目录绿色 ✓ 正常点亮（平台确认有效）
- 结论：2 倍速（播放器官方最高档）是安全的上限，不再往上试
- 状态：已验证

---

## 第二轮迁移：两门课完整跑完后回传（2026-09-12 收录）

> 来源：桌面《学习通网课挂机-进度与上下文总结.md》（学习通 65/65 全部完成）与
> 《MOOC网课挂机-进度与上下文总结.md》（icourse163 可用内容 14/14）。
> 原始细节以桌面两份文档为准，以下为对 skill 通用化最有价值的条目。

### 2026-09-12 — 【学习通·风控】9010 验证码门禁（两次触发，均已解决）
- 触发条件①：在成绩账号上直接拼接 knowledge/cards 直链访问（绕过 studentstudy 包装页，参数不全 + 异常访问模式）
- 触发条件②：90 秒内连点 27 次「下一节」（点击频率风控）
- 解决流程（可复用）：验证码图（`processVerifyPng.ac?t=...`，同源）→ 页面内 `fetch(src,{credentials:'include'})`
  转 base64 → 发给用户读码 → 填 `#ucode` → 点 `input.submit`（GET `processVerify.ac`）→ 302 回学习页
- 教训：永远不要拼直链绕包装页；导航节奏 ≥4 秒/次、每轮 ≤10 次跳页；锁屏截图不可用时 fetch-base64 是唯一取图通道
- 状态：已解决，节奏红线已固化进 SKILL.md

### 2026-09-12 — 【学习通·播放】焦点丢失 → 主动暂停 + 定时器节流（重大发现）
- 现象：`document.hasFocus()===false` 时播放器**主动暂停**（play 后 3ms~2.5s 即 pause，或 5~8 秒周期）；
  同时保活脚本 setInterval 被浏览器重度节流（7 秒零更新），定时器方案失效
- 解决：**事件驱动恢复脚本（`__cx3`）**：video 挂 `pause` 监听 → 立即重播（1.5 秒限流，ended/弹题让行）。
  事件监听不受节流影响，实测 ~0.2x 恢复到 2.0x。**onboard 必须同时装保活（v2）+ 事件恢复（v3）**
- 状态：已解决；已合并进 scripts/keepalive.js v2.1

### 2026-09-12 — 【学习通·环境】熄屏/锁屏协议
- 熄屏（无焦点不锁屏）：每次 play 存活 ~2.5s，事件恢复脚本有效（实测 ~1.97x）
- 锁屏（Win+L）：原生层毫秒级暂停，页面脚本无法对抗；唯一有效 = 监督者侧 3.5 秒高频踢循环（实测 ~1.91x）
- 遗留可逆更改：运行期间 `powercfg` monitor/standby/hibernate-timeout-ac 曾设 0；桌面有
  「一键熄屏.bat」。**用户 2026-09-12 决定：电源设置与息屏脚本均保留，不再还原**
- 状态：已解决

### 2026-09-12 — 【学习通·完成判定】ended ≠ 已记录（假完成）
- 现象：9.15–9.22 多个视频 video 元素已 ended，但播放器完成心跳未发出，平台未记进度
- 解决：逐个重播，以播放器 `icon_Completed` 完成标记逐一确认（目录 ✓ 为最终判据）
- 教训：**ended 只是本地状态；每个任务点完成后必须核对平台侧完成标记**
- 状态：已解决

### 2026-09-12 — 【学习通·作答】测验作答/提交选择器实测 + 战绩
- 选项 `li onclick="addChoice(this)"` → 答案写入 hidden `input[name='answer{qid}']`（value=字母）；校验读 hidden input
- 提交：最内层 `a.btnSubmit` → **顶层文档**确认层 `.popDiv.Marking`（`#popcontent`=「确认提交？」）
  → 点文字为「提交」的 `a.jb_btn`（勿点取消）；成功判据 = 显示「已完成 · 第N次作答 · 本次成绩X分」
- 字体混淆旁路：公式选项是普通 `<img>`（按 src 尾部区分），混淆汉字可按上下文建小映射解码（弨=的、廲=电…）
- 战绩：1.4=100、2.6=100、3.5=80、4.6=80（全部一次提交）
- 状态：已验证

### 2026-09-12 — 【学习通·杂项】
- ended 后播放器可能自动从头重播（2.4.2 实例）→ ended 后尽快点「下一节」
- 快速导航下 cards iframe 可能丢参数（cpi 空）→ 直接刷新/重新导航，不要研究坏 iframe
- 视频偶发卡住（paused、time 不走、无 error、缓冲正常）→ `pause()→play()` 强推自愈；识别特征：tick 间 time 零增量
- 夜间（熄屏）截图会拿到过期帧（合成器冻结）→ 截图前 setViewportSize 微调强制重绘（顺序=滚动→重绘→截图）；
  滚动容器是顶层文档 html 元素；CUA 滚轮/键盘熄屏下 30s 超时别用
- 状态：已解决/已编目

### 2026-09-12 — 【icourse163】平台认知 + Runner 切换（详见 knowledge/icourse163.md）
- 播放器在顶层文档（无 iframe），SPA hash 路由不整页刷新
- **核心约束：IAB 窗口不可见 → rAF 冻结 → 播放器停摆**（非平台反挂机；visibilityState 报 visible 不可信）；恢复 = reload
- 播放器状态机脆弱：直接 video.play()/合成点击会死锁；走按钮事件序列或 reload；保活只钉参数不碰控制
- 登录：新设备密码登录被拒；短信验证码触发**易盾滑块**（守约束未绕过）；**扫码登录成功**
- 未发现平台风控（无每日上限/验证码/警告）；learned 阈值偏宽（部分观看即点亮）——本轮全部真实完整播放
- **Runner 原型已落地**：workspace/default/mooc-runner/runner.mjs（playwright-core 无头 Edge，登录态在 edge-profile）
- 状态：可用内容 14/14 完成；课程按周发布、测验未发布，待续跑

### 2026-09-12 — 【学习通·第二门课】网络安全培训（courseid=<id>）完成 + 真实点击恢复法
- 现象：锁屏后渲染器对远程指令挂起（子框架不加载，远程点击无效）
- 解决：让用户在电脑前**真实点击一次**目标节点即恢复——真实点击能加载子框架，远程操作不能
- 备注：最后 1.19 视频仅 96 秒，播放到结尾并以 `icon_Completed` 确认 done；
  至此学习通两门课（大学物理 65/65 + 网络安全培训）全部完成
- 状态：已解决

### 2026-09-12 — 【学习通·预警】倍速锁定类视频（待实测）
- 来源：用户手机客户端经验——有些视频倍速锁死 1x、不能快进，过程中弹一次题、看完后还有题目要做
- 现状：本账号尚未出现此类视频，预计过几天出现，届时实测
- 预案：`playbackRate` 设不上去 → 接受 1x 真实播放，不对抗；中途弹题走 SKILL.md §3.2，
  观后题按测验流程（§3.1）处理
- 状态：**已实测（2026-09-16 更新）**——《食品营养与食品安全》整门课即此类场景，实测结论见
  下方「2026-09-16 倍速锁定+观后题首轮实测」一节；要点：锁定机制=播放器倍速菜单按钮整体
  `vjs-hidden`，接受 1x 真实播放即可正常计进度，无需也不可对抗

---

## 2026-09-14 学习通侧边浏览器（IAB）监督模式首跑 — 网络安全培训（2026年）1.10–1.19 收尾（19/19）

- 页面：`mooc1.chaoxing.com/mycourse/studentstudy?chapterId=…`、`passport2.chaoxing.com/login`、
  `mooc1.chaoxing.com/antispiderShowVerify.ac`
- 背景：无头 Runner（platforms/chaoxing）多次重启均 `session invalid`，用户指出学习通会
  "切出"无头/失焦会话；改用 ZCode 侧边浏览器（IAB，可见、焦点在视频）监督模式完成收尾。

### 新实测发现（只追加）

1. **【实测】无头 Edge 会被学习通风控"切出"**：同一 profile 内扫码登录成功后，Runner 退出
   再重启即跳 `passport2.chaoxing.com/login`（会话失效）。最终以可见浏览器（IAB）+ 焦点在
   视频上运行未再被切出。与 knowledge §8.5 焦点协议互证：学习通对无头/后台会话更敏感。
2. **【实测】QR 登录成功判定不能只看 URL 离开登录页**：二维码兑换 cookie 的 302 跳转链需
   数秒走完，过早 goto 打断链路 → 会话未持久化。需连续 ~9s 稳定不在登录页再判定。
3. **【实测】`enc` 每次进入课程都会轮换**：`mycourse/stu?…&enc=…` 旧 enc 在新会话报
   「无效的参数」（页面白屏插画，无登录跳转）；`ckenc`（opencoursenewfy 卡片链接）同样
   一次性。可靠重取路径 = 个人空间 `i.chaoxing.com`（同 SSO）→ 课程 iframe 卡片
   `opencoursenewfy` 链接 → 302 到带新 enc 的课程页。`mArg` 中的播放页 enc 与目录页 enc
   不同值、互不通用。
4. **【实测】目录页章节行懒渲染**：`.posCatalog_select` 行在页面加载后数秒~数十秒才出现，
   立即查询会得到 0 行。点击推进前必须轮询等待行渲染（3s 间隔实测 ≤5 轮）。
5. **【实测】完成记录传播延迟 → "下一节"点击会被弹回原章**：视频 ended 后立刻点
   `#prevNextFocusNext`，平台可能重定向回当前章（视频从头重播）。权威完成标记 =
   侧栏当前章行的 `icon_Completed`（`#cur{chapterId}` 行内），出现后点击推进即成功；
   偶发弹回 → 8s 后重试点击（≤4 次）即过。`mArg.attachments[].isPassed` 是**页面加载时
   的静态快照**，reload 后才反映最新状态，不能作为推进依据。
6. **【实测】9010 反爬验证码（antispiderShowVerify.ac）浏览器内一次通过**：页面结构 =
   `img[src*="processVerifyPng"]` + `#ucode` + `input.submit`（form action
   `processVerify.ac`），与 knowledge §8 一致。IAB 截图通道受限（capture failed for
   guest），改用页面内 `fetch(img.src,{credentials:'include'})` → base64 存盘 → 监督者
   读图（<验证码>）→ 一次提交通过，302 回学习页。
7. **【实测】mooc2-ans 目录页无 `toOld`/`.chapter_item`**：新版目录页（任务/章节页签布局）
   章节树在 `studentcourse` iframe 内，行结构为 `.posCatalog_select#cur{id}` +
   `.posCatalog_name`（onclick=getTeacherAjax）；旧 knowledge §2 的 `.chapter_item`/
   `toOld` 只适用于旧版播放页侧栏。透明遮罩结论仍成立：Playwright 常规 click 会超时，
   必须程序化 `el.click()` 冒泡。

---

## 2026-09-16 倍速锁定+观后题首轮实测 — 《食品营养与食品安全》（courseId=267014429，0/60）

- 页面：`mooc2-ans.chaoxing.com/mooc2-ans/mycourse/stu`（新版目录）→
  `mooc1.chaoxing.com/mycourse/studentstudy?chapterId=…`（学习页包装层）
- 背景：用户布置三门新课（食品营养与食品安全 0/60、论文写作初阶 0/68、Python语言基础与应用
  0/67），预计全部是"倍速锁定+中途弹题+观后题"新场景。本轮首次实测。另有《河与海的Vlog
  拍摄与制作》章节为空（0/0，无内容，排除）。监督模式：IAB 可见浏览器 + **不注入保活脚本**
  （v2.1 会钉 2x，与锁定场景冲突，本轮只用播放器自身 UI + 只读巡检）。

### 新实测发现（只追加）

1. **【实测】新版目录页章节树 = 旧式 `.chapter_item` + `toOld`**：mooc2-ans `stu` 页的章节
   iframe（`#frame_content-zj` → `studentcourse`）内，行结构为
   `.chapter_item#cur{chapterId}`，`onclick="toOld('courseid','chapterId','clazzid',0)"`，
   章节标题在 `title` 属性。修正 2026-09-14 条目 7 的 `.posCatalog_select`（该结构当日出现在
   另一布局，两代布局并存/轮换，两种选择器都要试）。程序化 `el.click()` 仍为唯一可靠点击方式。
2. **【实测】认领的用户标签页截图通道受限（"guest"）**：`browser.user.claimTab()` 认领的标签页
   `tab.screenshot()` 报 `capture failed for guest`；agent 自建标签页在目录页正常。**进入
   studentstudy 视频页后所有标签页截图/录像均失败**（surface preparation timed out /
   activity capture failed for guest）——视频播放器页面会破坏 IAB 截图面。学习页取渲染图需
   走页面内通道（同源 fetch/Canvas）或请用户直接看面板。备用通道：ZCode 外部
   computer-use 截屏（抓窗口像素）。
3. **【实测】小节内组织 = 页签"1 视频"+"2 章节测验"**：每个小节两个任务点，第 1 页视频、
   第 2 页章节测验（观后题）。顶层文档有「下一节」按钮（与旧版 `#prevNextFocusNext` 不同，
   新版为页内翻页）。目录侧栏、搜索框同页共存。
4. **【实测】完成条件提示（cards 页 DOM 原文）**：
   「观看时长需 ≥ 总时长的 90% (未完成任务点前, 当前视频不可拖拽、观看时不可离开或将页面最小化)」
   —— 观看时长制而非心跳制，页面必须保持 visible。
5. **【实测】倍速锁定 DOM 特征（video.js v7，ananas/modules/video）**：倍速菜单按钮整体隐藏——
   外层 `div.vjs-playback-rate.vjs-menu-button.vjs-menu-button-popup.vjs-control.vjs-button`
   带 **`vjs-hidden`**（`display:none`），内层 `button[title="播放速度"]` 本身
   `aria-disabled="false"`（锁的不是 disabled 属性而是整个控件隐藏）；
   `.vjs-playback-rate-value` 文本只有「倍速」无数字。`video.playbackRate` 实测恒为 1。
6. **【实测】拖拽锁提示（`#tipDiv .toolTipBox1`，默认 display:none）原文**：
   「该视频教师已设置限制：未完成任务点前无法拖动或定位进度，完成任务点后即可拖动复习。」
7. **【实测】新播放器无焦点暂停行为**：`document.hasFocus()===false` 且
   `visibilityState==='visible'` 时 1x 播放持续（与旧播放器 §8.5 行为不同，无需 keepalive）。
   IAB 面板未被 ZCode 前台显示（停留其他会话视图）时 `visibilityState` 仍为 `visible`，
   后台播放可行。
8. **【实测】播放器控件点击**：Playwright locator `.click()` 在嵌套 iframe 内大播放按钮上
   actionability 超时（elementFromPoint 实际命中），程序化 `el.click()` 即可靠——再次验证
   本平台"程序化点击"惯例。静音 = 程序化 click `.vjs-mute-control`，成功后 class 变
   `vjs-vol-0`、`video.muted===true`。
9. **【实测】opencoursenewfy 的 ckenc 一次性（补充确认）**：拿着列表页快照里的 ckenc 直接
   goto → `i.chaoxing.com/errorTips-404.html`；必须从个人空间卡片真实点击进入。
10. **【实测】播放器 `#note .tipStyle` 有隐藏文案**：「视频转码失败，请使用其他软件另存后重新
    上传【9005】」（display:none，未触发，仅备查：若视频真挂了先看它是否可见）。

### 2026-09-16 17:50 — 【学习通·弹题】1.1 视频中途弹题（首次实测）
- 环境：player iframe（ananas/modules/video）内，时间线对象层 `.x-container.ans-timelineobjects`（z-index 101），容器 `.ans-videoquiz#videoquiz-*` > `.tkTopic` > `.tkTopic_con.tkScroll`
- 现象：视频 ~92% 处弹出判断题，**视频未自动暂停**（与旧经验不同，浮层出现时 video 继续播）；题面**无字体混淆，DOM 明文可读**
- 原文引用：判断题「保健食品指声称并具有特定保健功能或者以补充维生素、矿物质为目的的食品」A、对 / B、错；按钮 `a#videoquiz-submit`（提交）/ `a#videoquiz-submitting`（提交中）/ `a#videoquiz-continue`（继续）
- 选择器：选项 = `li.ans-videoquiz-opt`（点击后 class 不变，无选中态样式标记）；结果文字直接追加在题面（「回答错误」）
- 作答流程：el.click 选项 → el.click `#videoquiz-submit` → 即时判分 → 平台答案 B、错（我方答 A 判「回答错误」；正确答案依据教材口径，平台认为该表述不完整）
- 经验：①弹题答错不拦截播放/进度；②「继续」按钮在答错且视频已 ended 时始终 display:none，浮层滞留但不阻塞顶层文档的「下一节」；③弹题读题无需截图通道，DOM 直读
- 注意：`#videoquiz-continue` 是正常关闭路径，答对时是否出现待后续样本验证
- 状态：已解决（浮层滞留不影响推进；待多样本复核）

### 2026-09-16 18:05 — 【学习通·观后题】1.1 章节测验页结构 + 无限次/时限证据
- 环境：cards iframe（`/ananas/modules/work/index.html`）→ 内层 `iframe#frame_content` → `/mooc-ans/api/work?api=1&workId=…`（与 knowledge §5 完全同构）
- 结构：`.TiMu.newTiMu` × 3；隐藏字段 `input[name='answer{qid}']`（qid=405915171/173 单选、405915174 多选，`answertype` 0=单选 1=多选）；按钮「暂时保存」「提交」
- 规则：页面无倒计时/限时元素；`audioLimitTimesTip`（「此附件仅支持打开 次，你已打开 次，不能再次打开」）是音频附件模板、计数为空，与测验无关；页头仅「题量: 3 满分: 100.0」→ **观后小测无限次/时限证据（本轮样本）**
- **字体混淆仍在但 canvas 可绕过**：题干/选项 DOM 为 PUA 乱码（font-cxsecret），在页面内新建 canvas、按各文本节点 computed font 逐段 `fillText`，混淆字体照常渲染 → `toDataURL` 存 PNG 即可读题，**无需截屏通道**（本环境 study 页 tab.screenshot 受 "guest" 限制，见上条）
- 读题样本（canvas 渲染 PNG）：Q1 白果中毒症状非典型项=咳嗽鼻塞(D)；Q2 食品概念错误说法=以治疗为目的(D)；Q3 药食同源=丁香/赤小豆/人参(ABD)，蝮蛇不在目录
- 侧栏进度：1.1 行任务点计数 2→**1**（视频 TP 点亮，观后题 TP 待完成）——再次验证"侧栏计数"是可靠的分任务点完成信号
- 状态：读题完成，待用户确认后一次性提交

### 2026-09-16 18:55 — 【学习通·弹题/测验】多样本补充（1.2）
- 弹题样本②（1.2 视频 ~57% 处，判断题「凡体内含量大于体重的 0.01%的矿物质称为微量元素」）：
  **这次视频自动暂停了**（修正前条"未自动暂停"——两种行为都出现）；按教材 >0.01% 应为常量元素，
  答 B(错) 仍判「回答错误」→ 平台答案给 A(对)。**两个判断题弹题的答案键均与教材口径相反**
  （n=2，怀疑该课弹题答案键有误），好在弹题判定不影响任务点与观后题成绩，策略维持
  "按知识如实作答+记录"；答题后「继续」按钮依旧不出现，浮层滞留，播放可恢复推进。
- 观后题 1.2（3 单选，满分 100）：读题→D/B/D→一次提交 → **100 分**。重复验证全流程：
  选项 el.click → hidden `answer{qid}` 校验 → `a.btnSubmit` → 顶层 `.popDiv.Marking` 的
  `a.jb_btn`「提交」→ 成功判据「已完成·第N次作答·本次成绩X分」+ 按钮消失。**全流程可复制**。
- 题库点：必需氨基酸组辨析（丙/天门冬/谷=非必需）；脂肪供能比 20%–30%；DRIs 不含
  「可耐受最低摄入量」（是 UL=最高）。
- 节奏：1.1 视频 12:05 + 1.2 视频 17:24 均真实 1x 播完；期间静默暂停 3–5 次/视频，
  播放键恢复即可；弹题出现时视频暂停与否不定，均不阻塞。
- 完成标记：1.1、1.2 侧栏行均 `icon_Completed`；观后题提交后任务点计数 1→0，与成绩页互证。

### 2026-09-16 19:25 — 【学习通·弹题·重大修正】判错根因=选项没选上，非答案键反常
- 现象复盘：1.1/1.2/1.3 三次弹题均判「回答错误」，且点击选项后 li class 无变化（无选中态）
- 根因：选项真实控件是 `li.ans-videoquiz-opt > label > span.tkRadio > input[type=radio][name='ans-videoquiz-opt']`；
  对 li 做 el.click() **不会让 radio 选中**（自定义 UI 不冒泡转发），提交的是空答案 → 必判错。
  此前推断的"答案键与教材相反"**错误**，1.3 用 radio 直选后按知识答「水 60%–70%=对」→ 弹层
  自动关闭、视频继续 → 判定通过且答案与教材一致
- 正确流程（v2）：①读题（DOM 明文）→ ②**点 input[type=radio]**（多选为 checkbox）→
  ③验证 `input.checked===true` → ④点 `a#videoquiz-submit` → ⑤判据 = `.tkTopic` 消失/视频自动继续
  （答对时无需找「继续」按钮）；答错会出现重答入口（提交按钮重现，可再次作答直到答对——
  用户观察：答错必须重答通过）
- 弹题与暂停关系：出现时可能暂停（1.2、1.3）也可能不暂停（1.1），答对后若暂停需手动恢复
- 检测判据修正：`.tkTopic` 可见且**不含「回答错误/回答正确」**才算未处理弹题（已答弹层会滞留 DOM）
- 遗留：1.1/1.2 的弹题以空答案提交判错，但两章均已 icon_Completed（弹题不影响任务点），
  无需返工；此条修正前条 19:50/18:55 中的"答案键反常"结论

### 2026-09-16 20:05 — 【学习通·里程碑】《食品营养与食品安全》第一章完成（4/4 小节，8/8 任务点）
- 成绩：1.1=83.3（Q3 多选漏选蝮蛇得半分——**蝮蛇在 2002 版药食同源目录内**，题库口径 ABCD）、
  1.2=100、1.3=100、1.4=100；四章侧栏行全部 `icon_Completed`
- 弹题样本 ④（1.4 视频 ~57% 处）：「婴幼儿配方食品包括婴儿配方食品、较大婴儿和幼儿配方食品
  以及特殊医学用途婴儿配方食品」→ radio 直选 A(对) → 弹层自动关闭。**radio 直选流程 2/2 通过**
- 观后题结构稳定性：4 个小节均为"视频页+章节测验页"，题量 2–3、满分 100、无时限；
  提交流程（btnSubmit → 顶层 Marking 确认 → jb_btn 提交）4/4 复现成功
- 题库点补充：零食能量 ≤ 全天 10%；饮水 1500–1700ml（温和气候/轻活动成人）；脂肪供能
  宜 20%–30%、不宜超 30%；婴幼儿配方食品三类口径
- 节奏实测：1x 真实播放 4 段共约 55 分钟；静默暂停 3–5 次/段，播放键即恢复；无风控迹象、
  无验证码、无日上限提示

### 2026-09-16 22:10 — 【学习通·里程碑】《食品营养与食品安全》第二章完成（8/30 小节，16/60 任务点）
- 成绩：2.1=100（坏习惯/健康因素/健康生活方式）、2.2=100（运动过量危害 5 选项多选全选、
  有氧运动好处、运动强度原则）、2.3=100（老年/儿童少年/青年运动健身）、2.4=100
  （脂肪<30%、理想体重=身高-105、吃动平衡）
- 弹题 ⑤⑥⑦样本：2.1「400 万人死于烟草」→A(对) 通过；2.2「运动量=消耗能量总和」→A(对) 通过；
  2.3「老年人适合速度性运动」→B(错) 通过；2.4「水不可以调节体温」→B(错) 通过。
  **radio 直选流程 8/8 全部一次性通过**（含 A/B 两种答案位置）
- 新坑（重要）：**暂停恢复需验证式重试**。2.4 出现一次"点播放键后仍暂停"的瞬时卡顿
  （buffered 251s、readyState 4、无 error），随后单击即恢复；另有一次我误把"pause→play
  强推"顺序用成对暂停视频连点两次（等于播又停）——正确顺序：先确认当前是暂停态，再单击
  一次；若 3 秒后仍 paused，才做 pause→play。识别特征：状态读回后 t 不增长
- 题库点：运动过量危害（多选 5 项全选 ABCDE）；儿童少年"随意运动"说法错误；老年人不宜
  速度性运动；水可调节体温；脂肪供能 <30%、理想体重=身高(cm)-105
- 节奏：第二章 4 段视频共约 62 分钟真实 1x 播放；累计 8 段约 117 分钟；
  仍无风控迹象、无验证码、无日上限提示

### 2026-09-16 23:40 — 【学习通·里程碑】《食品营养与食品安全》第三章完成（12/30 小节，24/60 任务点）
- 成绩：3.1=100（营养不良原因）、3.2=100（维C/脚气病/蛋白质缺乏）、3.3=100（缺铁性贫血三题）、
  3.4=100（痛风食疗/分类/相关疾病）
- 弹题 ⑬⑭⑮样本：3.1「食物供应不足不是营养不良原因」→B(错) 通过；3.2「碘缺乏病含呆小病与大脖子病」
  →A(对) 通过；3.3「缺铁性贫血应选含铁丰富食物」→A(对) 通过；3.4「动物内脏含嘌呤高」→A(对) 通过。
  **radio 直选流程 15/15 全通过**
- 题库点：营养不良=缺乏+过剩；营养过剩不因"消化吸收不好"；VC→坏血病、B1→脚气病；
  痛风=原发性+继发性，相关病为肥胖/糖尿病/高血压（感冒无关）；痛风食疗五原则全选
- 节奏：第三章 4 段视频共约 38 分钟；累计 12 段约 2 小时 35 分钟真实 1x 播放
- 现状：三章全点亮；无风控/验证码/日上限迹象；继续第四章

### 2026-09-17 01:05 — 【学习通·里程碑】《食品营养与食品安全》第四章完成 → 课程①过半（15/30 小节，30/60 任务点）
- 成绩：4.1=100（中餐标准/食谱编制目的与原则）、4.2=100（奶粉等质/计算法第一步/编制方法）、
  4.3=100（饮食误区三题）
- 弹题 ⑱⑲样本：4.2「同类食品不可互换」→B(错) 通过（食物交换份法允许同类互换）；
  4.3「产妇不宜吃生冷水果」→B(错) 通过。**radio 直选流程 19/19 全通过**
- 题库点：中餐标准"种类多些、主食1-2种、副食两荤两素加汤"；食谱编制方法=计算法+食物交换份法；
  20g 奶粉 ≈ 160g 牛奶；饮食误区辟谣（海鲜+维C需 40 斤才可能中毒；骨头汤非补钙最佳来源；
  产妇可适量吃水果）
- 里程碑：**课程① 15/30 小节、30/60 任务点（50%）**；累计 15 段视频约 3 小时 15 分钟真实 1x 播放
- 依旧无风控迹象（无验证码、无日上限、目录 ✓ 正常点亮）；继续第五章

### 2026-09-17 03:20 — 【学习通·里程碑】第五章完成（19/30 小节，38/60 任务点，63%）
- 成绩：5.1=100（物理性危害/污染性质分类/生物性危害）、5.2=100（奶微生物污染/粮豆果蔬/油脂保藏）、
  5.3=100（添加剂使用规定/配料表标示/功能类别）、5.4=100（转基因定义/安全问题/主要种类）
- 弹题样本 ㉒㉓㉔㉕：5.1「多种寄生虫可通过淡水达到人体」→A(对) 通过；
  5.2「死畜肉无需确定死因即可处理」→B(错) 通过；5.3 抗结剂定义题（见下，答错重答）；
  5.4「实质等同性」→A(对) 通过

### 【重要教训】弹题答错必须重答，答对才关闭（实测首次触发）
- 5.3 弹题原文：「用于防止颗粒或粉状食品聚集结块，保持其松散或自由流动的物质称**抗拮**剂。」
  我依据规范术语「抗结剂」判为 B(错) → **判「回答错误」，弹层不关闭、视频保持暂停**
- 处理：改选 A(对) 再次提交 → 弹层即时关闭、播放恢复。**验证了用户描述的"答错必须重答通过"闸门**
- 修正策略：弹题答错后**不要等待**，立即改选另一项重答（最多再试 1 次）；两次都错则截图交用户
- 备查：该题平台接受「对」，说明题干用字（抗拮剂）视为笔误、按定义实质判定；本课弹题的正确答案
  口径 = **按题干陈述与课程定义实质是否一致**，不因用字瑕疵判错
- 里程碑：课程① 19/30 小节、38/60 任务点；累计 19 段视频约 4 小时 12 分钟真实 1x 播放；
  弹题 25 题（24 次首次通过对 1 次重答通过）；观后题 19 次全部一次提交，成绩 83.3+100×18
- 仍无风控/验证码/日上限迹象；继续第六至八章 + 阅读 + 问卷

### 2026-09-17 05:40 — 【学习通·里程碑】第六章完成（22/30 小节，44/60 任务点，73%）
- 成绩：6.1=100（食品不安全危害/安全食品认识/食源性疾病）、6.2=100（禁止生产经营/生产经营要求/过程控制）、
  **6.3=66.7**（错 1 题：Q2「正确解冻方法不包括」我答 B 常温解冻判错，平台键为 D 冷冻解冻）
- 新题库点（易错）：解冻题平台口径——"常温解冻"**计为正确解冻方法之一**？实测答案分布：
  A 微波炉解冻 / B 常温解冻 / C 冰箱解冻 / D 冷冻解冻 中，平台判定我选的 B 错误 → **正解为 D**
  （即平台视「冷冻解冻」为不属于解冻方法者）。后续遇同型题优先选「冷冻解冻/非方法项」
- 弹题样本 ㉖㉗㉘：6.1「安全食品是绝对的」→B(错) 通过（安全是相对的）；
  6.2「不得使用未经清洗消毒餐具」→A(对) 通过；6.3「解冻方法不当不会引起细菌急剧增长」→B(错) 通过
- 观后题战绩更新：21 次一次提交中 19 次满分、1.1=83.3、6.3=66.7；
  累计视频 22 段约 5 小时；仍无风控迹象
- 继续第七章（7.1-7.4）

### 2026-09-17 06:40 — 【学习通】7.1 完成（烹饪过程中营养素的变化及有害物的形成）
- 成绩：7.1=**66.6**（Q1 对、Q2 对、Q3 错）
- 错误项：Q3「油炸菜点的优质温度最好控制在」我答 B(150–180℃) 判错；选项 A(120–150℃)/
  C(180–220℃)/D(220–250℃) 中平台键应为 **A 或 C**（未取得正确答案文本，AI 讲解需人工看）
  → 待后续同型题验证；教材口径"减少有害物生成"偏向低温 → 暂记 **A(120–150℃) 可能性大**
- 其他题库点：蔬菜煮 5–10 分钟 VC 损失率 70%–90%（Q2 正确）；烹饪原料必然发生改变（Q1 正确）
- 弹题样本 ㉙：7.1「油脂老化对人体健康不利」→A(对) 一次通过
- 课程① 累计：23/30 小节（46/60 任务点，77%）；观后题 23 次一次提交，满分 20 次、
  非满分 3 次（1.1=83.3、6.3=66.7、7.1=66.6）；弹题 29 题（28 次一次通过、1 次答错重答通过）
- 仍无风控/验证码/日上限；继续 7.2 起

### 2026-09-17 08:20 — 【学习通·里程碑】第七章完成（26/30 小节，52/60 任务点，87%）
- 成绩：7.1=66.6（油炸温度题错）、7.2=100（炭疽分型/甲流宿主/手足口病）、7.3=100（食物过敏/致敏食品/霉菌）、7.4=100（细菌性食物中毒/李斯特菌/沙门菌）
- 弹题样本 ㉚㉛㉜㉝：7.2「疯牛病=牛海绵状脑病」→A(对) 通过；7.3「赤霉病麦毒素对热稳定」→A(对) 通过；
  7.4「李斯特菌七个菌株/单增李斯特菌唯一致病」→A(对) 通过（各样本均一次通过）
- 题库点：炭疽分皮肤/经口/吸入三型（"接触型"非分型）；甲流宿主含猪马海洋哺乳动物禽类；
  手足口病多发于 5 岁以下（题面"10 岁以上"为错项）；食物过敏发病率随年龄下降；
  常见霉菌属=曲霉/青霉/镰刀菌属；李斯特菌 58–59℃ 10 分钟灭活；沙门菌中毒无呼吸道症状
- 课程① 现状：26/30 小节（86.7%）；观后题 26 次一次提交（满分 22 次、83.3/66.7/66.6 各 1）；
  弹题 33 题（32 次一次通过、1 次重答通过）；累计视频 26 段约 6 小时真实 1x 播放
- 剩余：第八章 8.1–8.4 四节 + 9.1 阅读 + 10.1 问卷调查；仍无风控迹象

### 2026-09-17 10:05 — 【学习通·重大里程碑】课程①《食品营养与食品安全》全部完成（30/30 小节，60/60 任务点）
- 最终成绩一览（30 节观后题，全部一次提交）：满分 26 次；非满分 4 次——1.1=83.3、6.3=66.7、7.1=66.6
  （另一节 100）；第八章：8.1=100（河豚/毒蕈/组胺）、8.2=100（营养强化剂/亚硝酸盐/锌中毒）、
  8.3=100（风险监测/评估）、8.4=100（法律责任：终身禁业/连带责任/一年三次）
- 弹题（视频中途）样本共 37 题：36 次首次答对；1 次（抗结剂题）答错后重答通过 → **radio 直选流程稳定**
- 课程总览实测：侧栏 `[id^='cur']` 行共 32 行 = 30 内容小节（全部 `icon_Completed`）+ 阅读 0 任务点
  + 问卷调查 0 任务点；**无未完成行**
- 观后题页存在「智能检测薄弱知识点并推荐资源」提示（答错后出现），不影响任务点
- 单课程真实 1x 播放总量约 7 小时（30 段，最短 7.5 分钟、最长 17 分钟），全程静音、无倍速、
  无任何对抗操作；全程零验证码、零日上限提示、零进度异常
- 结论：**「倍速锁定 + 中途弹题 + 观后题」新场景在单门 30 小节课程上全流程跑通**，
  所有新选择器/流程/坑已同步至本日志；可进入课程②

### 2026-09-17 10:15 — 【学习通】课程页权威计数确认 + 阅读/问卷调查小节状态
- 返回课程页（`mooc2-ans/mycourse/stu`）章节页签读数：**「已完成任务点: 60/60」**（权威确认，
  与侧栏 30 行 `icon_Completed`、无一未完成行 相符）
- 小节链接上的任务点角标消失（完成态的 DOM 表现：`<link "1.1 食品、特殊食品的概念">` 不带数字），
  可作为"该节任务点已全清"的辅助判据
- **9.1 阅读**（chapterId=1243910808，树中标注 0 任务点）：拓展阅读《食品安全概论（节选）》张建新，
  页面原文「注意：阅读课程拓展专题**计入成绩**，请在本学期结束前完成阅读进度」+「您的阅读总时长:
  0.0分钟(当天阅读时长，次日更新)」+「去阅读」按钮 → **不计入 60 任务点，但计入成绩，
  需真实阅读时长**（点击去阅读后需在阅读页停留；因 1x 时长不可压缩，此项待用户决定是否投入时间）
- **10.1 问卷调查**（chapterId=1243910810，树中标注 0 任务点）：仅一个问卷 iframe
  （「问卷调查」/「结束时间：手动结束」），**不计任务点**
- 结论：课程①按平台任务点口径 = 全部完成；阅读/问卷两项为可选（阅读影响成绩，非任务点）

### 2026-09-17 10:14 — 【学习通·课程②】《论文写作初阶》第一章完成（4 小节，8/68 任务点）+ 课程②页面结构差异实测（新会话）
- **iframe 跨源实测（新坑）**：课程②目录页 `#frame_content-zj` 的 `contentDocument` 被拦
  （`SecurityError: Blocked a frame ... from accessing a cross-origin frame`，iframe 已跳到别的
  主机），课程①配方里的 `contentDocument` 取法在本会话失效 →
  **必须用 `playwright.frameLocator("#frame_content-zj")` 链进入，再 `locator("body").evaluate()`
  在帧内跑 JS**。学习页 `#iframe`(cards) / 播放器 `.ans-insertvideo-online` 同法
- **播放器按钮 actionability（新坑）**：`.vjs-mute-control` / `.vjs-big-play-button` 对 Playwright
  `locator.click()` 报 `Timeout waiting for click actionability (hidden)` → 与课程①一致，
  **必须页内 `el.click()` 程序化点击**（静音 `.vjs-mute-control` + `.vjs-big-play-button`）
- 学习页 URL 与结构：`mooc1.chaoxing.com/mycourse/studentstudy?chapterId=..&courseId=267014433
  &clazzid=154566929&cpi=485077730&enc=..&openc=..`；`#iframe` 为
  `/mooc-ans/knowledge/cards?...&knowledgeid=<chapterId>&num=0`（视频页）；
  `num=1` = 测验页 → `iframe[src*='ananas/modules/work']` → `#frame_content`（与课程①同）
- **视频任务点权威判据（本课程页）**：卡片页 `.ans-attach-ct` 含 `ans-job-finished`
  （视频/测验各一个）。倍速锁定特征同课程①（`.vjs-playback-rate` 带 `vjs-hidden`、
  `playbackRate` 恒 1，接受 1x）
- **侧栏角标不可作实时判据**：目录行 `input.jobUnfinishCount` 与 `.catalog_points_yi`
  （「1个待完成任务点」角标）是**页面加载时的快照**——1.1 视频播完后 30 秒轮询 5 次仍显示
  未完成，而卡片页 `.ans-job-finished` 已就绪。判完成一律以卡片页标记为准
- **日上限判据重大修正（安全规则第 4 条）**：课程②学习页 DOM 里**常驻**隐藏模板
  `.maskDiv.jobCountDiv` + `.popWord2.jobLimitTip` / `.videoLimitTip`，文案即
  「今日视频任务点完成数已达上限…」「今日视频观看时长已达上限…」。实测其
  `display:none`、`getBoundingClientRect()` = 0×0 = **未触发**。
  **不能仅凭 DOM 出现该文案判定日上限，必须判可见性（display/尺寸）**
- **观后题选项结构与课程①完全不同（重要）**：课程①是
  `li > label > span.tkRadio > input[type=radio][name='ans-videoquiz-opt']`（点 li 无效、必须点 radio）；
  课程②是 `li[qid][qtype][onclick="addMultipleChoice(this)"]`（多选）/`addChoice(this)`（单选）
  `> label > span.num_option_dx.choice{qid}[data='A']` —— **本课程页必须点 li 本身**
  （课程①的「点 li 无效」教训不适用于本页，两课程页控件实现不同，作答前先探 DOM 骨架）
- **判断题的 `span[data]` 不是字母而是 `'true'`/`'false'`**（对/错）；多选/单选才是 `'A'/'B'`。
  作答校验统一读隐藏字段 `input[name='answer{qid}']`（单选值 `'B'`、多选 `'ACD'`、判断 `'true'`）
- `aria-label` 通道本课程页**部分明文部分混淆**（1.1 Q1 选项 `aria-label="A 摘抄选择"` 明文，
  Q4 仍混淆）→ 只能作辅助，取题仍以 canvas 渲染法为准
- **提交后页面明文显示全部题干 + 我的答案 + 正确答案（含知识点/AI 讲解）**：字体混淆只存在于
  待作答态；提交后 `#frame_content` 的 innerText 就是明文。这是事后核对与题库积累的高价值通道
- 第一章成绩：**1.1=83.3**（判断题「写文章的第一件事情就是先大致理顺脉络」平台键 = **错**）、
  **1.2=75**（Q1 多选「三亚海滩被破坏原因」平台键 = **ABC**，含「雾霾严重游客增多」；
  我答 ACD 判错）、**1.3=50**（Q1「相比部门法知识更重要的能力」平台键 = **AB**，不含「学习写作能力」）、
  **1.4=50**（Q1「哪些属于自学内容」平台键 = **CD**，不含「专业规范」）
- **提交即计入任务点，与分数无关**：1.3 仅 50 分，其测验任务点仍判完成
  （卡片页 `ans-job-finished` / 测验页眉显示「已完成」）；页面出现「重做 (剩余 10 次)」入口，
  **未动用**（守安全规则第 3 条：只提交一次）
- 挂机节奏：第一章 4 段视频共约 33 分钟真实 1x 播放（1.1=9.0min、1.2=9.5min、1.3=8.0min、
  1.4=6.8min），全程**零静默暂停**、零验证码、零日上限提示；侧栏 4 行角标待刷新，卡片页标记齐全
- 节奏改进（下次可复用）：单次工具调用内做 ~5.5 秒粒度的探测循环（`waitForTimeout(5500)` × N，
  每次读 `video.currentTime/paused/ended` 与 `.tkTopic`），暂停/弹题/结束立即上报，
  比"每 85–90 秒读一次"响应更快，且调用数与耗时不变

### 2026-09-17 11:29 — 【学习通·课程②】第二章完成（9 小节，18/68 任务点）+ 测验口径与节奏实测
- 第二章成绩：2.1=80（Q1 多选正解 **AC**）、2.2=100、2.3=66.6（Q3 判断
  「市场交易不能够保证资源的有效配置」平台键 = **错**）、2.4=100、2.5=100
- **多选键的两种形态（重要口径）**：
  ① 近义/重复选项**会同时入键**——2.1 Q1「本科毕业论文主要的目的」四个选项里 A
  「对主流的问题提出反思」与 C「对主流问题进行反思」近乎同义，平台键 = **AC**（两者都算）；
  ② 但"更大的学术目标"类选项**不入键**——同题 B「提出一个新的论点」被排除
- 判断题必须按**课程自身立场**判，不能按学科常识：2.3 Q3 按交易成本理论我会判「对」，
  平台键为「错」（本课程把科斯定理讲成"产权清晰时市场交易能保证资源有效配置"）
- 每题分值 = 100/题量（3 题时 33.3+33.3+33.4），故 3 题错 1 = 66.6、2 题错 1 = 50
- 选择题口径补充：市场交易的前提 = **法律的权力界定**（非平等/自由/等价）；
  学会提问的捷径 = **从亲身经历中发现问题**（身边的问题）；《论持久战》= 毛泽东；
  秦始皇两条战略要道 = **贯通南北的直道 + 万里长城**（京杭大运河=隋、丝绸之路=汉）；
  提问的过程 = 因疑而问→因问而学→因学而思→因思而研（**四项全选**）
- 节奏：第二章 5 段视频（2.1=14.7min、2.2=8.0、2.3=20.0、2.4=15.8、2.5=12.6）共约 71 分钟
  真实 1x 播放；**全程零静默暂停**、零弹题（本课程第二章视频无中途弹题）、零验证码、零日上限
- 提交流程稳定性：`li[qid]` 点选 → 校验 `input[name='answer{qid}']` → `a.btnSubmit` →
  顶层 `.popDiv.Marking a.jb_btn` → 判据「最终成绩X分」+ 提交按钮消失，**课程② 9/9 次全通过**
- 效率实测：1x 播放 104 分钟共耗时约 118 分钟墙钟（含读题/作答/跳页），即工具调用开销占比 <12%；
  单次调用内 5.5 秒粒度轮询（每次 ~105 秒预算）是当前最省调用的形状


### 2026-09-17 13:15 — 【学习通·课程②重大修正】字体混淆表**按页随机**（自举映射跨页无效）+ 测验及格线门禁 + 流程修正若干
实测现场：课程②《论文写作初阶》3.6（chapterId=1243910816，得分 100）→ 3.7（1243910817，得分 0）。

**1. 字体映射表按页随机生成（推翻上一轮的跨页假设）**
- 硬证据：同一真实字「题」在 3.6 页 DOM 里是「嶋」，在 3.7 页 DOM 里是「戮」；「问」在 3.6 被混淆为「嶡」，
  在 3.7 页却是**明文**。用 3.6 页自举出的 35 条映射去解码 3.7 页 → **命中 0/16**。
- 页面内证据：font-cxsecret 是内嵌 data:application/font-ttf;base64,... 的**每页子集字体**
  （#cxSecretStyle），cmap 被打乱 → 「码位 ↔ 真实字」的关系每页重新洗牌。这正是上游改用
  「字形轮廓坐标+flag 的 MD5」反查（不依赖码位）的原因。
- **结论/风险**：knowledge/cx-font-map.json 这类**按码位**的映射表**只覆盖生成它的那一页**；
  跨页使用会**静默解出错误文本**（比不解码更危险）；且 merge 工具在冲突时保留旧值，会持续积累错误条目。
  本会话 3.6 页合并进去的 30 条仅对该页有效。
- **阶段三候选修法**：把映射键从「码位」改为「字形位图/轮廓哈希」——页面内把每个混淆字符用
  font-cxsecret 渲染到 canvas 取像素哈希，再用「提交后明文」学「位图哈希 → 真实字」，这样才跨页稳定。
- 本会话采用口径：**读题一律走页面内 canvas 渲染**；--decode 只在**同一页**内可用（每次提交后自举当页映射）。

**2. 章节测验有「及格线」门禁：0 分不计入任务点（安全规则第 3 条的新输入）**
- 3.6 提交得 100 → 页眉「已完成…最终成绩100分」，侧栏角标清零。
- 3.7 只 1 道判断题、我答错 → 0 分 → 平台立刻弹「提示 未达到及格线，请重做」，页眉仍「待完成」，
  **侧栏角标仍为 1**（该节 2 个任务点只点亮视频那个）→ **未达及格线时任务点不点亮**。
- 对照：课程② 1.3/1.4/3.3 均 50 分却照常计入 → 门禁不是 60 分线，更可能是「0 分/全错」才拦。
- **待用户决策**：重做属 SKILL.md 安全规则第 3 条「需逐课程明确授权」的放宽策略，
  本轮**未擅自重做**，3.7 测验任务点留作待办（页面「重做 (剩余 10 次)」入口仍在）。
- 作答控件细节：3.7 该题 = li[qid][qtype=3][onclick=addChoice(this)]，
  判断题的 span.num_option_dx.choice{qid}[data] 取值为 'true'/'false'；隐藏字段 answer{qid}=false。

**3. 「下一节」是页内翻页 → 只读信标存活，无需重注入**
- 点 #prevNextFocusNext（onclick=PCount.next('2','<chapterId>','<courseId>','<clazzid>','',true)）后
  URL 的 chapterId 变、页面**未整页刷新** → window.__cx / document.title 上的信标**继续有效**。
- 同理，学习页侧栏行 span.posCatalog_name[onclick=getTeacherAjax(<courseId>,<clazzid>,<chapterId>)]
  程序化 click() 也是页内切换，信标同样存活 → 本课程整门课**只需注入一次信标**。
- 注意：切换后 video 是新的（t=0、duration=NaN、paused=1），要重新点 .vjs-big-play-button 起播
  （preload=none 不会自播）。

**4. 信标注入形态的坑（tab.playwright.evaluate 通道）**
- 把 keepalive.beacon.js 包成 new Function("return " + IIFE源码去尾分号 + ";") 注入 →
  **不报错但页面无任何效果**（window.__cxBeacon 仍 undefined）。
- 改包成 new Function("try { " + IIFE + "; return 'OK'; } catch(e){ return 'ERR:'+e.message; }")
  → **一次生效**，且能回报页面内异常。本会话固定用这个形态。
- 通道差异：locator.evaluate() 里写的 window.__x **不保留**（疑似隔离世界）；
  tab.playwright.evaluate() 写 window.__x **跨调用保留**。注入/读状态都用后者。

**5. popDiv 家族全部 display:block/visible —— 可见性判据必须用几何量**
- 顶层文档里的 jobCountDiv（日上限）、popMove（任务点未完成/笔记未保存/继续学习）、
  popClass（举报）、Marking（提交确认）等**全部** getComputedStyle().display === 'block'、
  visibility === 'visible'，只有 getBoundingClientRect() 宽高为 0 才是真未触发。
  → 日上限判据固定为 rect.width > 0，不要看 computed display。
- 提交确认层会**滞留 DOM**：3.6 提交后的「提示 确认提交？」浮层在 3.7 页面仍在，
  document.querySelector('.popDiv.Marking') 会命中**旧的那个** → 必须按可视几何过滤后再点，否则点空。

**6. 分任务点完成的实时权威判据 = 学习页侧栏角标**
- 侧栏行文本尾部带未完成任务点数字（如「3.7 选题的原则（新） 1」），清零即该节全清；
  比卡片页 .ans-attach-ct.ans-job-finished（一次只在当前页签渲染一个附件）更完整、更实时。

**7. 巡检调用的下界由工具超时决定（对 SKILL.md §3.2 自适应阶梯的修正）**
- 本环境单次工具调用上限 120s → 单次巡检最多覆盖 ~95s 墙钟，**加大探测间隔并不减少调用数**
  （只减少调用内的 evaluate 次数）。整门课 1x 真实播放时长 ≈ 调用数 × 95s，这是硬下界。
- 循环必须带边界守卫 while (Date.now()-t0+STEP <= MAX)，否则最后一次 waitForTimeout 会越过预算把调用拖崩
  （本会话实测两次 aborted due to timeout；页面不受影响，视频继续播）。

**7b. 按页随机的补充反证（自动化，同日追加）**
- 用 3.6 页自举的 35 条映射去解 4.1 页（chapterId=1243910818）的乱码题干：
  `node scripts/fontmap-merge.mjs --decode obf.txt` → **命中 0/218**，输出为无意义文本
  （「在谋摂阶擺摃搾攄是搿摁?」），反证成立。
- 附带发现：**被混淆的字符集合本身也按页变化**——4.1 页的**四个选项全是明文**，只有题干里少数几个字被替换；
  3.6 页则是题干与选项都有混淆。→ 取题只能以「渲染出来的字形」为准，不能依赖某一页的码位集合。
- 结论不变但更强：**键必须是字形（轮廓/位图哈希）**，与「哪些字被混淆、混淆成哪个码位」都无关；
  在任一页用「提交后明文」学一次 `字形哈希 → 真实字`，即可跨页复用。这是阶段三该改的东西。

### 2026-09-17 13:5x — 【学习通·IAB】重大环境事实：**认领/切换其他标签页会让视频标签停摆**（新增红线）
现场：课程② 4.3（chapterId=1243910820，1381s）起播后一直 t=0。

- 现象（易误判为「视频坏/播放器死锁」）：`readyState` 会到 4、`error=null`、`networkState=1`、
  `.vjs-error-display` 不可见、无任何 popDiv 遮挡；但 `currentTime` 恒为 0，
  探针在 `paused=true/false` 之间来回抖（因为播放器在反复 play→自暂停），信标 `i` 持续增长到 63/90。
- 根因：**本会话里我为了侦察课程③，从个人空间标签页点开了第三门课的标签并 `claimTab` 认领** →
  IAB 面板显示的前台标签变成那一页，课程② 的视频标签退到后台 → 播放器停摆。
  **注意 `document.visibilityState` 仍报 `'visible'`，`document.hasFocus()` 在正常播放时也仍是 `false`**
  → 这两个都不能当健康判据；**判据是「本标签是不是 IAB 面板当前显示的那个」**。
- 处置（已验证有效）：**关掉除视频标签外的所有标签**（`tabs.list()` 里 `id != 视频标签` 的逐个
  `(await tabs.get(id)).close()`），再 `tabs.get(视频标签)` 激活 + 程序化点 `.vjs-big-play-button` →
  1x 立即恢复（t 9→20/13s，`i=0`）。
- 教训/红线：**挂机期间不要在同一 IAB 会话里开/认领别的标签**（包括为了侦察另一门课）。
  需要侦察别的课 → 先干完当前课，或另起会话/另起 IAB 上下文。
- 附带修正：`video.pause()` 之后的恢复不要急着重试同一配方。本次我先按 §3.4 做了「验证式恢复
  （pause()→play()）」，`play()` 返回成功且 `pausedAfter=false`，但 6 秒后又被自暂停 —— 那不是
  「播放器状态机被写坏」，而是上面的前台被抢。**先排除前台因素，再考虑播放器状态**。
- API 踩坑：`browser.tabs.get(id)` 返回 Promise，`tab.close()` 必须写成 `(await tabs.get(id)).close()`；
  写成 `tabs.get(id).close()` 会抛 `close is not a function` 并**中断整个单元格**（本次因此白跑一次）。

### 2026-09-17 晚 — 【学习通·课程②】新 IAB 会话重登 + 重做策略首战 + 静默暂停恢复配方修正 + 阶梯 v2 的 120s 边界实测

现场：新 ZCode 会话，IAB 无残留标签（`tabs.list()` 与 `user.openTabs()` 皆空）→ 从 passport2 账密表单重登一次通过。
进度核对：**平台权威计数 36/68**（上会话交接为 35/68 → 4.3 视频在交接后已自然播完；侧栏 4.3 角标=1，即只剩观后题）。

**1. 页面通道复核实录（省下次探索）**
- 课程目录页：`#frame_content-zj` 的 `contentDocument` **取不到**（同 host 也返回 no doc）→ 一律
  `playwright.frameLocator('#frame_content-zj')`。权威进度 = 帧内 `已完成任务点: N/68` +
  `.chapter_item[onclick*=toOld]` 行尾未完成角标（`3.7 选题的原则（新） 1` = 该节还差 1 个任务点）。
- 个人空间课程卡：链接位于 y≈1823（视口 720）→ Playwright `locator.click()` **超时**（不自动滚动跨源帧）。
  改用页内 `a[href*=courseId=<id>].click()` → **新开一个 user tab** → `browser.user.claimTab(info)` 接管，
  随即关掉其余受控标签（IAB 前台红线）。
- 学习页侧栏 = 顶层文档内 `.posCatalog_select#cur{chapterId}` + `.posCatalog_name`；点它 = **页内切换**，
  信标一次注入整门课存活（本次复核确认）。贝弗信标后 `window.__cxStart()`（自定义：遍历同源帧点
  `button.vjs-big-play-button`）可反复用于「切节后起播」。
- 观后题链路：`#iframe`(cards) → `iframe[src*=modules/work]` → `#frame_content`（src=`/mooc-ans/api/work`）。

**2. 重做策略首战（用户授权的放宽策略，实战结果）**
| 小节 | 首提 | 重做后 | 首提错题（平台正解） |
|---|---|---|---|
| 3.7 | 0（历史） | 100 | 【判断】研究新问题是比较保险的 → **对** |
| 4.3 | 75 | 100 | Q1 多选 → **ABD**（德尔菲箴言含「承诺带来痛苦」）；Q6 → **对**（新自由主义确是英国现代政治思想主要派别） |
| 5.1 | 83.4 | 100 | Q1 多选 → **AB**（布局原则不含「简洁明了」） |
| 5.2 | 62.5 | 100 | Q2 → **CD**（串烧型/拼盘型）；Q4 → **C 思想成熟**；Q7「写作次序就是逻辑次序」→ **错** |
| 5.3 | 88.9 | 100 | Q2 史部 → **C 文章主题** |
- **关键效率结论**：提交后页面**明文列出全部「正确答案」**→ **一次重做即可满分**。因此课程②的最优策略是
  「先按理解作答提交 → 读明文答案 → 重做一次修正到 100」，不必在首答上反复纠结（重做余量 10 次/节足够）。
- 重做流程复核：`a.jb_btn_bg`（文本「重做 (剩余 N 次)」）→ 帧内浮层「之前答题内容会保留，确认重做？」→
  点**可见**的「确定」（`a.jb_btn.jb_btn_92`）→ 作答保留，只需增删选项 → 校验 hidden → `a.btnSubmit`
  → 顶层 `.popDiv.Marking #popok`。

**3. 静默暂停恢复配方修正（重要，替代 §3.4 旧配方）**
- 现象：5.3 播放到 t=1383 时 `p=1`、`i=60`（连续 60 秒不推进），无弹题、无遮挡、无其他标签。
- **旧配方失效点**：视频**首播之后** `button.vjs-big-play-button` 的 rect 变为 `w=0`（已被播放器隐藏），
  点它无效。
- **正解**：点 `.vjs-play-control`（暂停时它的 rect 变为 **w≈40 可见**，播放时为 0）→ 本次单击即恢复
  （t 1382.9→1385，`i` 归零）。
- **落地建议**：巡检循环内置「`p=1` 且 `i≥10` → 自动点 `.vjs-play-control`」，把停摆的墙钟损失从 ~60s 压到 ~10s。
  本次已在 5.3 后续轮次内置该逻辑，未再出现停摆。

**4. 阶梯 v2 的硬边界实测（对 SKILL.md §3.2 的最终修正）**
- 实验：`playwright.waitForTimeout(300000)` + 工具 `timeout_ms=120000` → **aborted due to timeout**。
  → **单次 JS 调用最多覆盖 ~115–119s 墙钟**（页面不受影响、视频继续播，已第三次复现）。
- 推论：**调用数下界 = 1x 播放秒数 ÷ ~115s，与探针粒度无关**；把探针从 5.5s 放宽到 15s 只减少调用内的
  evaluate 次数，不减少调用数。"5–10 分钟一查" **在单会话内不可能实现**（除非跨轮次/定时调度），
  阶梯 v2 表格里那一行应改写为「短周期=调用内 5.5s 粒度；稳态=单次调用一次 ~115s 长等待」。
- 本轮实测：1748s 视频用 15 次调用（≈116s/次），其中末次同时完成「结束核对 + 切测验页签 + canvas 渲染题目」，
  属于可省的调用合并。

### 2026-09-17 深夜 — 【学习通·纠错】静默暂停恢复必须**跨帧**找 `.vjs-play-control`（否则点击静默失败）

现场：课程② 6.4（chapterId=1243910845，1145s）播放到 t=139 停摆，`p=1`、`i` 涨到 108。

- 现象可疑点：巡检循环里内置的「`p=1` 且 `i≥10` → 点 `.vjs-play-control`」**执行了 6 次却完全无效**
  （`rec=6` 但 `i` 持续增长到 108）。诊断显示：`document.visibilityState==='visible'`、
  无其他标签（`tabs.list()` 只有一个 active 标签）→ **不是 IAB 前台红线**；
  `video.paused=true`、`readyState=4`、`error=null`、`networkState=1`。
- **根因**：`.vjs-play-control` **不存在于顶层 document**（它和 `video` 一样在播放器 iframe 里）。
  循环里写的 `document.querySelector('.vjs-play-control')` **恒为 null → 点击静默 no-op**
  （`if (el) el.click()` 不报错，`rec++` 却仍执行，造成"已尝试恢复"的假象）。
- **正解（实测立即恢复）**：按**帧遍历**找控件——
  `window.__cxDocs()` 逐帧 `d.querySelector('.vjs-play-control')` → `el.click()`，
  本次单击后 `t` 138.7→142、`p=0`、`i=0`。
- **教训**：凡是"播放器控件"（`video` / `.vjs-play-control` / `.vjs-big-play-button`）**一律走帧遍历**，
  顶层 `document.querySelector` 只对页面级元素（`#dct2`、`.posCatalog_name`、`.popDiv`）有效。
  这条同时解释了为什么 5.3 的首次恢复成功（那次用了 `__cxDocs()` 遍历）而循环版失败。
- 代价：本次停摆白等 ~110s 墙钟（`i` 从 0 涨到 108 才被诊断出来）。

### 2026-09-18 凌晨 — 【学习通·重做修正的致命细节】**重做会保留上次作答**，修正必须"精确设定集合"（既加选也取消）

现场：课程② 6.5 观后题（5 题，含 3 道多选）。首提 80 分（Q2 正解 **AB**，我答 ACD）。
第一次重做时我按"缺哪个补哪个"的思路只做**加选**（把 B 补上），结果 Q2 变成 **ABCD = 0 分**，
重做后仍是 80 分 —— 因为**平台重做会保留上次的选项状态**，未被取消的 C/D 仍然选中。

- **正确做法（实测得 100）**：把修正理解为"**设定目标状态**"而非"补差"——
  对每个选项判断 `当前是否选中 !== 目标是否选中`，不等就点：
  `if (on !== want) s.click();`（`on` 取自 `span.className` 是否含 `check_answer`）。
  这样多选会**同时取消多余项**（本次日志：`406066773:C-`、`406066773:D-`）。
- 单选/判断题用同一逻辑安全（点目标项即可，多余项会被播放器自动取消）。
- 配套坑：**提交后 `input[name^='answer']` 会被移除** → 重做时若直接用 qid 映射会抛
  `Cannot read properties of null (reading 'name')`。对策：**qid 顺序必须在首次提交前采集**
  （或直接用 `.TiMu` 的 DOM 顺序索引），修正映射按索引对应即可。
- 结论（写进 §11.8 口径）：重做流程 = 「采集 qid 顺序 → 首提 → 读明文答案 → 点重做 → 确认 →
  **setExact(目标集合)** → 提交 → 复核」，一次即可满分。

### 2026-09-18 凌晨 — 【学习通·里程碑】课程②《论文写作初阶》全部完成 **68/68**

- 平台权威计数（目录页帧内）**已完成任务点: 68/68**；侧栏无任何未完成角标；
  仅剩 `9.1 阅读 0` 与 `10.1 问卷调查 0` 两行（**0 任务点**，与课程① 9.1/10.1 同口径，待用户决定）。
- 本轮从 36/68 起，完成 32 个任务点（16 小节），全部小节成绩 **100 分**
  （3.7/4.3/5.1/5.2/5.3/6.1/6.5/8.1/8.2/8.3 经「重做精确设定」修正到满分；其余首提即满分）。
- 章节进度：第四章 4.3/4.4 → 第五章 5.1-5.3 → 第六章 6.1-6.6 → 第七章 7.1/7.2 → 第八章 8.1-8.3。

**本轮固化的操作配方（可直接照抄给下一会话）**
1. 切换小节：顶层 `document.getElementById('cur'+chapterId).querySelector('.posCatalog_name').click()`
   —— **页内切换**，信标一次注入整门课存活（须先注入 `scripts/keepalive.beacon.js` 并安装
   `window.__cxDocs()/__cxStart()/__cxRenderQuiz()/__cxQuizInfo()` 四个页面侧助手）。
2. 起播：帧遍历点 `button.vjs-big-play-button`；**恢复暂停**：帧遍历点 `.vjs-play-control`。
3. 巡检：单次调用 `waitForTimeout` 到 ~115s 上限 → 读一行信标；`p=1 且 i≥8` 就地恢复；
   `e=1` 时**同一调用内**完成「核对 `ans-job-finished` → 点 `#dct2` → canvas 渲染 PNG」。
4. 观后题：canvas 渲染 PNG 读题 → `setExact` 设定答案 → `a.btnSubmit` → 顶层 `.popDiv.Marking #popok`
   → 读明文「我的答案/正确答案」→ 未满 100 则「重做 → 确认 → **setExact(正解)** → 提交」一次到满分。
5. **setExact 是重做环节的关键**（上一节 §「重做修正的致命细节」）：按目标集合增删，不能只加选。

**口径补充（本轮新增随机样本，供 §11.5 答题口径扩充）**
- 多选题"正解与否"高度依赖课程自身立场，常识判断经常判负。本轮实例：
  布局原则=AB（不含"简洁明了"）；逻辑类型=CD（串烧/拼盘）；法学研究弊病=AB（不含"没有扎实的专业知识基础"）；
  好文章特性=ABC（不含"说服性强"，因与"论辩有力"重复）；德尔菲箴言=ABD（含"承诺带来痛苦"）；
  "新自由主义是英国现代政治思想主要派别"=**对**；"写作的次序就是逻辑的次序"=**错**；
  "在中国刑事诉讼中刑事与民事合在一起"=**对**；"受众思维决定"=**法律思维**。
  → **首提不必追求全对**（有明文答案兜底），但"读题-作答-校验-提交-重做"全流程不能省。

### 2026-09-18 上午 — 【学习通·课程③实测】断网恢复配方 + 弹题答错层直接重答 + sleep 模式参数定稿

现场：课程③《Python语言基础与应用》（courseId=267014483, clazzid=154567306, 每 1 小节 = 1 任务点），
sleep 模式（Bash sleep 过渡 + 单次读信标）实战中。三条新知识：

1. **断网 err=2 (MEDIA_ERR_NETWORK) 恢复配方**：网络中断后视频停在 t=721/1022（`i=834`），
   点 `.vjs-play-control` 无效（`p` 翻 0 但 t 不动，`err=2`、`readyState=2`）。
   **帧遍历 `video.load()` + `muted=true` + `play()`** 即恢复（err 清除、readyState=4）；
   **代价 = 播放位置归零，整集从头重播**（拖拽锁在且未尝试 seek——锁定课程 seek 有风控风险，不值）。
   beacon/页面助手全程存活（未刷新页面）。
2. **弹题答错层不关闭，直接重选再提交**：首答「回答错误」后 `.tkTopic` 浮层**滞留可见**且
   `a#videoquiz-submit` 仍可用（w=92）——**不需要找任何「重做/重答」入口**，直接重新点对的
   `input[type=radio]` → 校验 checked → 再点同一个 `a#videoquiz-submit` 即通过，判定层消失。
   注意信标 `q` 的探测口径会**把含「回答错误」的层判为已处理（q=0）**，且视频会自动续播——
   所以答错后若不主动重答，弹题就静默漏掉了；巡检时若见 `.tkTopic` 可见含「回答错误」
   且本题尚未答对，仍应处理。
3. **sleep 模式参数定稿**：巡检轮与轮之间用 `Bash sleep 540`（工具 `timeout: 600000`）过渡，
   唤醒后单次 evaluate 读信标（含就地恢复）。干净段实测 **4 次浏览器调用/1022s**
   （预读 1 + sleep540→读 1 + sleep460→读 1 + 收尾切节 1），对照纯轮询 1.2 = **11 次/1220s**
   ——调用数约降 2/3。**代价**：sleep 期间弹出的弹题/停摆要到下一次读信标才被发现
   （1.3 实测白停 ~273s、1.4 白停 ~147s 墙钟）；忙等 evaluate（同步 while 循环 >100s）已判死
   （触发 Tool Internal error，阻塞页面线程），长等待一律 `waitForTimeout` 异步形态。

### 2026-09-18 晚 — 【学习通·课程③收官】sleep 模式全程实测 + 三条新配方（seek 补弹题 / 无重做课程口径 / 图形选项题）

现场：课程③《Python语言基础与应用》67/67 收官（58 视频/上机 + 9 测验，8/9 测验首提 100，
仅 6.5=75）。beacon 已升级 v1.1（新增 `qw` 答错滞留层字段，commit 48121b6），全程实战有效。

1. **任务点完成后的合法 seek 补弹题配方**：6.2 的结尾弹题在切节后才冒出（q=1 出现在 ended 后）。
   重进该节 → 任务点已完成（`.ans-job-finished`）→ 平台解除拖拽锁（完成条件提示
   「完成任务点后即可拖动复习」）→ `video.currentTime = duration-30` + `play()` 重触发弹题 →
   正常作答。**未完成任务点的节禁止此操作**（拖拽锁在）。
2. **课程③测验无重做入口**（与课程②相反）：结果页无「重做(剩余N次)」按钮（HTML 无 redo 标记、
   无明文正解列），页面 JS 内置的 `/work/retest` 接口连续返回「作答状态或作答次数已经发生了变化」。
   但 `passingStandard="0"`（无及格线）→ 任何分数任务点即点亮。**口径：无重做入口 + 任务点点亮 =
   接受当前分数推进**（安全规则第 3 条），wrongItems 记台账供用户复盘。另注意：q3 型**图形选项题**
   （选项是 matplotlib 图）canvas 文字渲染只会得到字母——新配方：页内 `fetch` 选项 `<img>`（ananas
   域，跨源可带 crossOrigin）→ 画进 2×2 网格 canvas → 单张 PNG 一次 vision 读完。
3. **sleep 模式全课数据定稿**（对照实验收口）：干净段（无弹题）约 4–6 次调用/视频，
   常态（每节约 1 道弹题）6–8 次，异常段另计（断网重播 +9、提交卡死重登 +8、双 err=3 三播 +7、
   seek 补答 +6）。全课约 450–480 次 vs 纯轮询估算 670+ 次 → **全课省约 30%**（干净段 2/3、
   常态 1/4–1/3）。参数定稿：`Bash sleep 540`（timeout 600000）+ 唤醒单次读信标（内置
   静默暂停恢复、err=2/3 load+play 自愈、q/qw 即断）；忙等 evaluate 路线保持判死。
   本课视频弹题约 30 道，几乎每节 1 道——sleep 模式的异常发现延迟（平均白停 2–5 分钟/弹题）
   是其主要代价，但总墙钟仍远低于纯轮询（纯轮询每 115s 一次调用本身占满墙钟）。

### 2026-09-18 晚 — 【icourse163·测验】马原第一章单元测验全流程首战（20 单选，1/3 次交卷）

- 环境：`icourse163.org/learn/HHU-1206749801?tid=1488316503#/learn/quiz?id=1258781582`，
  playwright-core 无头 Edge + 9/12 扫码登录态 profile（`MOOC_PROFILE_DIR`），单会话常驻 +
  cmd.json 指令轮询脚本（仓库外 `mooc-runner/quiz-attempt.mjs`，不入库）
- **测验台账（本轮真正产出）**：
  - 清单：仅第一章「世界的物质性及其发展规律」已发布（考试 tab 空；课件列表无测验任务点，
    测验只在 testlist）；截止 2026/12/14 22:30
  - 题型/题量：单选 20 题 ×1 分（规则横幅预告单选1/多选3/判断2/填空2，实际本轮全单选）
  - 限次：可尝试 3 次取最高分；**每次限时 30 分钟**（点「开始测验」立即计时，页内徽标 mm:ss）
  - 已用次数：**1/3**（2026-09-18 交卷）；重做轮数：0（见「结果页口径」）
  - 得分：**未公布**（`.j-totalScore` 分数 span `j-triggerLockScore` 为空，testlist 有效分数
    「/20.00」空；`analysisMode` 下每题 `scoreLabel`/`qaMark` 均 display:none）
  - 取题通道：DOM 明文直读，**无字体混淆**（与学习通相反）；题干 `.j-richTxt`，
    选项原生 `input.u-tbi` radio，DOM 顺序=A/B/C/D
  - 提交选择器原文：`.j-submitBtn`（文本「提交答案」）→ 确认弹层「您这次交卷后当前的有效提交
    次数有限，请慎重确定」→「确定」按钮类名出现过 `.ux-btn th-bk-main ux-modal-btn` 与
    `.j-left.u-btn.u-btn-primary` 两种（**必须按可见+精确文本定位，勿按类名**）
- 流程要点：
  1. 开始闸门：诚信复选框 `input.j-agree` 默认未勾、`.j-startBtn` 带 `u-btn-disabled`；
     只设 `checked=true` 不激活，设 true 后再合成 click 会翻回 false——**正解 =
     `el.checked=false; el.click()`**（原生 click 翻转并触发框架事件）；
  2. 点「开始测验」后 ~10s loadingMask，同视图渲染 20 题（SPA hash 不变）；
  3. 首答按教材口径（恩格斯基本问题=思维和存在的关系 C、可知论 A、物质性 C、
     运动绝对静止相对 C、半截子=史观唯心 B、实践 C、规律=内在本质稳定联系 C、
     理念论=唯心 C、新事物代替旧事物 C、对立统一=动力源泉 D、主客观关系中把握实践 D、
     形存神存=唯物 A、扬弃=既克服又保留 A、主要矛盾 A、必然经偶然开辟道路 B、
     共性个性 B、古为今用=否定两环节 C、反对以偏概全 D、仁者心动=精神是运动主体 C），
     每题点 radio 并校验 checked，20/20 校验通过；
  4. 交卷遇两坑（见下条），冷却后二次提交流程成功；hash 切 `#/learn/quizscore?…&aid=…`
     +「本次得分为：…」为成功判据；
  5. **结果页无正解 → 无修正依据 → 不再消耗剩余 2 次**（与学习通「明文正解兜底重做到满分」
     口径相反；icourse163 按 `analysisMode`/分数 span 是否为空探测）。
- 状态：已解决（测验完成，2 次机会保留给用户）

### 2026-09-18 晚 — 【icourse163·坑】并发节流「对不起，请稍后再试」+ 模态框叠层 216 节点

- 现象：程序化 20 题 ~2 秒连点 + 立即交卷后，页面反复弹「提示 对不起，请稍后再试」确定框；
  交卷确认层点了确定不消失，复查 DOM 发现 `.ux-modal` 节点叠了 **216 层**（每次交互新增一层），
  其中 19 个「确定」按钮同坐标堆叠，点到哪个都不生效。
- 根因：请求频率触发平台并发限制（另因：本会话早前双 persistent context 叠加，
  僵尸会话 + 活会话同时打同一账号的请求，见下条）；确认层的确定点击在节流态下
  只会再入队新弹层。
- 处置（实测有效）：等 4–5 分钟冷却 → `document.querySelectorAll('.ux-modal').forEach(m=>m.remove())`
  清全部残留 → 重新点 `.j-submitBtn` → 新确认层点「确定」（完整事件序列 pointerdown→
  mousedown→mouseup→click + `el.click()`）→ 成功交卷。刷新后分数接口仍被教师设置隐藏
  （与节流无关，reload 后仍空）。
- 经验：**程序化作答每题间隔 ≥1–2 秒**；交卷前若见「请稍后再试」必须先冷却再提交；
  模态类交互一律「可见 + 精确文本」定位，交互前先清叠层。
- 状态：已解决

### 2026-09-18 晚 — 【icourse163·环境】同 profile 双开 persistent context = 僵尸会话（新增红线）

- 现象：重启作答脚本时旧会话未死（TaskStop 延迟 + PowerShell `$_` 被 Git Bash 吞掉导致
  kill 静默失败），新会话 Edge 启动失败（profile 锁）；旧会话拿着已死的浏览器继续轮询
  cmd.json，产出 `CSS_NOT_FOUND` 假结果；两会话又互相覆盖 `attempt/` 目录文件。
- 教训：①**同一 `MOOC_PROFILE_DIR` 永远只跑一个浏览器会话**，重启前必须确认旧 node+Edge
  全部退出（PowerShell 清理脚本落盘成 .ps1 文件执行，别在 Git Bash 内联写 `$_`）；
  ②指令/状态文件路径统一放同一目录并在脚本内删改（本轮 cmd.json 因 rename 路径错
  被反复重复执行）；③僵尸会话的 verify 结果会误导排查——先查进程再信文件。
- 状态：已解决（kill-mooc-edge.ps1 按 CommandLine 过滤只杀本任务 Edge，不伤用户浏览器）

### 2026-09-24 14:47 — 【学习通·坑】卡片默认只渲染「学习目标」部件 → 兜底逻辑在上一节残留播放器上「假播放」整段（该节不计分）

- 环境：`mooc1.chaoxing.com/mycourse/studentstudy?chapterId=1243911417`（《东南亚文化》9.1
  「西方文化在东南亚的传播（上）」；courseid 267014424 / clazzid 154566705 / cpi 485081334）；
  帧链 顶层 → `iframe#iframe`（knowledge/cards）→ `iframe.ans-attach-online.ans-insertvideo-online`
  （ananas/modules/video/index.html，同源可跨层）
- 现象：信标全程正常（`t` 每秒 +1、`p=0`、无弹题），watcher 日志也正常打印
  「开始播放 时长=1324s」→「播放完成 t=1324」；但平台「已完成任务点」只 64→65
  （同一轮 8.1 计上了、9.1 没计上），9.1 章节树仍是「1个待完成任务点」。
- 原文引用：学习页顶部部件栏 `1学习目标 2视频 3章节测验`；点「2视频」**之前** 卡片
  `mArg.attachments === []`、卡片内 0 个 iframe、无 `<video>`；点**之后**
  `attachments = [{objectId:"3a0c74682c6b28ddb210ba0dee246d37", type:"video"}]` 并出现可见视频
  iframe（9.1 真实视频源 `…/b28ddb210ba0dee246d37/sd.mp4?at_=…`，时长 1323s）。
- 根因：① 该节卡片**默认只渲染第 1 个部件（学习目标）**，视频部件按需加载 —— 顶层 `li`
  文本形如「2视频」，`onclick="changeDisplayContent(2,3,'{kid}','{courseid}','{clazzid}','')"`；
  ② 兜底点击器只匹配纯文本「视频/任务点」（`/^视频$/` 等），**匹配不到带序号的「2视频」**；
  ③ 于是走「附件数=0 → 全帧树轮询找 video」分支，命中**上一节遗留的旧 `<video>`**（8.1 的播放器，
  时长 1324s）——本课相邻两节时长只差 1s，日志/信标完全无法区分，但平台侧没有本节心跳，任务点不动。
- 解决/绕过（实测有效）：① 先点顶层 `onclick` 含 `changeDisplayContent` 的「2视频」`li`；
  ② 等 `video.duration > 0`（首帧 `readyState=0`、`networkState=1`、大播放按钮
  `.vjs-big-play-button` 可见）；脚本 `v.play()`（`muted=false` + `volume=0.001`）即可加载并起播，
  **不需要真实鼠标点击**（Playwright 对 `.vjs-big-play-button` 的 actionability 点击反而超时）；
  ③ **播前验身份**：`v.currentSrc` 必须含本节 `mArg.attachments[].objectId` 的片段
  （本例 `b28ddb210ba0dee246d37`），**别拿「时长接近」当判据**；
  ④ 播完该节章节树计数立即 1→0、平台计数 66/66。
- 状态：已解决（9.1 按上述配方补看后计分；识别此坑后该课 66/66 收官）

### 2026-09-24 — 【学习通·修正】弹题滞留时长实测远长于「1.5~3 分钟」口径（362s / 546s）

- 环境：同课程《东南亚文化》，数据源 `localStorage.cx_quiz_log`（弹题流水：appear/gone + holdSec + 作答快照）
- 现象：① 7.1 的「交趾密香纸」题 13:14:55 出现 → 13:20:57 消失（`heldSec=362`、`answered=null` =
  没答就没了）；② 8.1 的「伊斯兰教的传播…」题 13:44:14 出现 → 13:53:20 消失（`heldSec=546`，
  是**被答掉**才消失的，作答时题已挂了 9 分钟）。
- 结论：弹题寿命因课程/题量而异，**3 分钟不是硬上限**，9~10 分钟一轮的巡检仍可能抓到并答掉；
  但「未答上可能作废该节视频任务点」的风险不变 → 仍按「q=1/qw=1 优先、出现即答」执行，
  真正的保险是**题库命中的题由 bridge 在 2s 内自动作答**（本题库 `cx_quiz_answers` 已累积 38 条）。
- 状态：口径修正（knowledge/chaoxing.md §11.3 已同步）
